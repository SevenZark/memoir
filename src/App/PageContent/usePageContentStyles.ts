import { createUseStyles } from 'react-jss';

const usePageContentStyles = createUseStyles({
    root: {
        padding: 15
    },
    instructions: {
        fontSize: 16,
        textAlign: 'center'
    }
});

export default usePageContentStyles;